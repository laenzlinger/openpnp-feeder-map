version: "2"

run:
  timeout: 3m

linters-settings:
  errcheck:
    check-type-assertions: true

  govet:
    enable-all: true
    disable:
      - fieldalignment
    settings:
      shadow:
        strict: false

  funlen:
    lines: 80
    statements: 60
    ignore-comments: true


  lll:
    line-length: 150

  nakedret:
    max-func-lines: 0

linters:
  disable-all: true
  enable:
    # defaults
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    # extra
    - asciicheck
    - bodyclose
    - copyloopvar
    - dupl
    - durationcheck
    - errname
    - errorlint
    - funlen
    - goconst
    - gocritic
    - gocyclo
    - goprintffuncname
    - lll
    - nakedret
    - nestif
    - nilerr
    - nolintlint
    - unconvert
    - unparam
    - whitespace

issues:
  max-same-issues: 50
  exclude-rules:
    - path: "_test\\.go"
      linters: [funlen, dupl, gosec]
    # cobra requires globals and init()
    - path: "cmd/"
      linters: [gochecknoglobals, gochecknoinits]
