version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - asciicheck
    - bodyclose
    - copyloopvar
    - dupl
    - durationcheck
    - errname
    - errorlint
    - funlen
    - goconst
    - gocritic
    - gocyclo
    - goprintffuncname
    - lll
    - nakedret
    - nestif
    - nilerr
    - nolintlint
    - unconvert
    - unparam
    - whitespace
  settings:
    errcheck:
      check-type-assertions: true
    govet:
      disable:
        - fieldalignment
    funlen:
      lines: 80
      statements: 60
      ignore-comments: true
    lll:
      line-length: 140
    nakedret:
      max-func-lines: 0

issues:
  max-same-issues: 50
  exclude-rules:
    - path: "_test\\.go"
      linters: [funlen, dupl]
